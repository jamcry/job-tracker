<div class="jumbotron col-md-6">
  <h1><%= @job.description %> <span class="badge badge-pill badge-success"><%= @job.duration %> Mins</span></h1>
  <h5><b>Added at: </b><%= @job.created_at %></h5>
</div>
<hr>
<%= link_to 'Edit', edit_job_path(@job) %> |
<%= link_to 'Back', jobs_path %>
