<div class="jumbotron col-md-6">
  <h1><%= @job.description %></h1>
  <h5><span class="badge badge-pill badge-success"><%= @job.duration %> Mins</span> <span class="badge badge-primary"><%= @job.category.name %></span></h5>
  <h5><b>Added at: </b><%= @job.created_at %></h5>
</div>
<hr>
<%= link_to 'Edit', edit_job_path(@job) %> |
<%= link_to 'Back', jobs_path %>
